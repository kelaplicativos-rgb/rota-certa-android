name: Apply local backup patch

on:
  push:
    paths:
      - .github/workflows/apply-local-backup-once.yml
      - .github/patches/local-backup-98ac46e.patch.b64
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply patch and remove temporary files
        run: |
          set -euo pipefail
          base64 -d .github/patches/local-backup-98ac46e.patch.b64 > /tmp/local-backup.patch
          git apply --check /tmp/local-backup.patch
          git apply /tmp/local-backup.patch
          rm .github/patches/local-backup-98ac46e.patch.b64
          rm .github/workflows/apply-local-backup-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/br/com/mapeiaia/rotacerta/MainActivity.kt app/src/main/java/br/com/mapeiaia/rotacerta/Models.kt app/src/main/java/br/com/mapeiaia/rotacerta/Repositories.kt .github/patches/local-backup-98ac46e.patch.b64 .github/workflows/apply-local-backup-once.yml
          git commit -m "Add local backup and restore"
          git push
