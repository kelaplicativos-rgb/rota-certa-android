name: Runtime Bubble UI

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
  push:
    branches:
      - codex/pr5-stable-bubble-combined

permissions:
  contents: read

jobs:
  runtime-ui:
    name: Install, tap and verify bubbles on Android
    runs-on: ubuntu-latest
    timeout-minutes: 55
    env:
      ANDROID_AVD_HOME: ${{ github.workspace }}/.android/avd

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.10.2

      - name: Prepare runtime evidence directory
        run: |
          mkdir -p runtime-ui "$ANDROID_AVD_HOME"
          echo "RUNTIME_STAGE=prepared" > runtime-ui/progress.txt
          echo "HEAD_SHA=${GITHUB_SHA}" >> runtime-ui/progress.txt
          echo "ANDROID_AVD_HOME=${ANDROID_AVD_HOME}" >> runtime-ui/progress.txt

      - name: Build APK for runtime simulation
        env:
          GOOGLE_MAPS_API_KEY: ${{ secrets.GOOGLE_MAPS_API_KEY }}
        run: gradle assembleDebug --no-daemon --stacktrace --console=plain

      - name: Install Android emulator image and libraries
        run: |
          set -euxo pipefail
          sudo apt-get update
          sudo apt-get install -y libpulse0 libgl1 libx11-6 libxcomposite1 libxcursor1 libxi6 libxtst6 libnss3
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "emulator" "system-images;android-34;default;x86_64"
          mkdir -p "$ANDROID_AVD_HOME"
          echo no | avdmanager create avd --force --name rota_runtime --package "system-images;android-34;default;x86_64" --device pixel_2
          "$ANDROID_SDK_ROOT/emulator/emulator" -version > runtime-ui/emulator-version.txt 2>&1
          "$ANDROID_SDK_ROOT/emulator/emulator" -list-avds | tee runtime-ui/avd-list.txt
          grep -Fxq 'rota_runtime' runtime-ui/avd-list.txt
          test -x "$ANDROID_SDK_ROOT/emulator/emulator"

      - name: Start emulator and run universal-address regression
        id: runtime
        continue-on-error: true
        env:
          RUNTIME_UI_DIR: runtime-ui
          RUNTIME_APK: app/build/outputs/apk/debug/app-debug.apk
        run: |
          set -uo pipefail
          EMULATOR_BIN="$ANDROID_SDK_ROOT/emulator/emulator"
          test -x "$EMULATOR_BIN"
          "$EMULATOR_BIN" -list-avds | tee runtime-ui/avd-list-before-start.txt
          grep -Fxq 'rota_runtime' runtime-ui/avd-list-before-start.txt

          if [ -e /dev/kvm ]; then
            sudo chmod 666 /dev/kvm
            ls -l /dev/kvm | tee runtime-ui/kvm.txt
          else
            echo "KVM_NOT_AVAILABLE" | tee runtime-ui/kvm.txt
          fi

          echo "RUNTIME_STAGE=emulator_launching" >> runtime-ui/progress.txt
          "$EMULATOR_BIN" @rota_runtime \
            -no-window \
            -noaudio \
            -no-boot-anim \
            -no-snapshot \
            -wipe-data \
            -gpu swiftshader_indirect \
            -accel auto \
            > runtime-ui/emulator.log 2>&1 &
          emulator_pid=$!
          echo "$emulator_pid" > runtime-ui/emulator.pid

          cleanup() {
            adb emu kill >/dev/null 2>&1 || true
            kill "$emulator_pid" >/dev/null 2>&1 || true
            wait "$emulator_pid" >/dev/null 2>&1 || true
          }
          trap cleanup EXIT

          sleep 5
          if ! kill -0 "$emulator_pid" >/dev/null 2>&1; then
            echo "RUNTIME_STAGE=emulator_died_before_test" >> runtime-ui/progress.txt
            cat runtime-ui/emulator.log
            exit 1
          fi

          echo "RUNTIME_STAGE=script_invoking" >> runtime-ui/progress.txt
          set +e
          bash -x scripts/runtime_bubble_ui_test_bounded.sh > >(tee runtime-ui/runtime-script.log) 2>&1
          runtime_status=$?
          set -e
          echo "RUNTIME_SCRIPT_EXIT=$runtime_status" >> runtime-ui/progress.txt
          exit "$runtime_status"

      - name: Upload real Android screenshots and validation
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: runtime-bubble-ui-validation
          path: runtime-ui/
          if-no-files-found: error

      - name: Require successful universal-address runtime simulation
        if: always()
        run: |
          echo "Runtime action outcome: ${{ steps.runtime.outcome }}"
          test "${{ steps.runtime.outcome }}" = "success"
          test -f runtime-ui/runtime-validation.txt
          grep -Fq 'IN_APP_TOGGLE=approved' runtime-ui/runtime-validation.txt
          grep -Fq 'UNIVERSAL_TWO_ADDRESS_RUNTIME=approved' runtime-ui/runtime-validation.txt
          grep -Fq 'PACKAGE_SELECTION_REQUIRED=false' runtime-ui/runtime-validation.txt
          grep -Fq 'MODEL_REQUIRED=false' runtime-ui/runtime-validation.txt
          grep -Fq 'DESTINATION_WITHOUT_MODEL=present' runtime-ui/runtime-validation.txt
          grep -Fq 'BUBBLE_STATE=active_not_gray' runtime-ui/runtime-validation.txt
          grep -Fq 'DESTINATION_RULE=last_complete_numbered_address' runtime-ui/runtime-validation.txt
          grep -Fq 'FLOATING_RUNTIME=approved' runtime-ui/runtime-validation.txt
          test -s runtime-ui/app-before.png
          test -s runtime-ui/app-after-off.png
          test -s runtime-ui/app-restored-on.png
          test -s runtime-ui/home-bubble.png
          test -s runtime-ui/unregistered-two-address.png
          test -s runtime-ui/after-address-clear.png
          test -s runtime-ui/floating-menu.png
