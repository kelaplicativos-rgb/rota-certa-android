name: Stage47 Step7 run locator (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow retired
        run: echo "Historical Stage47 locator retired; preserved in Git history and excluded from automatic pushes."
