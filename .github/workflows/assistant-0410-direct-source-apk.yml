name: Rota Certa Assistant 0.1.414 vehicle read APK (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow retired
        run: echo "Historical 0.1.414 workflow retired; preserved in Git history and excluded from automatic pushes."
