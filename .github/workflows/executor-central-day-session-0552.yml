name: Executor Central do Dia + SessionKeeper 0.1.552 (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow retired
        run: echo "Historical 0.1.552 materializer retired; preserved in Git history and excluded from automatic pushes."
