name: Agenda global trace materializer retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: echo 'Global Agenda trace is materialized directly in app/src and validated by Direct-source branch APK.'
