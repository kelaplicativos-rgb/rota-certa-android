name: Rota Certa 0.1.506 Agenda card expansion stability

on:
  push:
    branches:
      - agent/agenda-card-expansion-stability-0.1.506
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: agenda-card-expansion-0506-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-build:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    env:
      GOOGLE_MAPS_API_KEY: agenda_card_expansion_0506_ci_dummy_key
      FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
      FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
      GOOGLE_APPLICATION_CREDENTIALS_JSON: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
      FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
      BASE_AUTHORITY_SHA: 81fb201ff02438cfc4805e6954113a59faf748be
      EXPECTED_VERSION_CODE: '5798'
      EXPECTED_VERSION_NAME: '0.1.506'

    steps:
      - name: Checkout exact triggering HEAD
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Prove lineage and protected surfaces
        shell: bash
        run: |
          set -euo pipefail
          SOURCE_HEAD="$(git rev-parse HEAD)"
          test "$SOURCE_HEAD" = "$GITHUB_SHA"
          printf '%s\n' "$SOURCE_HEAD" | tee /tmp/source-head.txt
          printf '%s\n' "$GITHUB_REF_NAME" | tee /tmp/source-branch.txt
          echo "ROTA_CERTA_GIT_SHA=$SOURCE_HEAD" >> "$GITHUB_ENV"
          echo "ROTA_CERTA_GIT_BRANCH=$GITHUB_REF_NAME" >> "$GITHUB_ENV"
          grep -Fq "versionCode = $EXPECTED_VERSION_CODE" app/build.gradle.kts
          grep -Fq "versionName = \"$EXPECTED_VERSION_NAME\"" app/build.gradle.kts
          FAROL='app/src/main/java/br/com/mapeiaia/rotacerta/LiveRideAccessibilityService.kt'
          git diff --exit-code "$BASE_AUTHORITY_SHA" -- "$FAROL"
          git diff --exit-code "$BASE_AUTHORITY_SHA" -- trip-platform/firebase.json trip-platform/firestore.rules
          git diff --name-only "$BASE_AUTHORITY_SHA"...HEAD | tee /tmp/changed-files.txt
          ! grep -Fxq "$FAROL" /tmp/changed-files.txt
          ! grep -Fxq 'trip-platform/firebase.json' /tmp/changed-files.txt
          ! grep -Fxq 'trip-platform/firestore.rules' /tmp/changed-files.txt
          sha256sum "$FAROL" | tee /tmp/farol.sha256

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate public Agenda regression contracts
        shell: bash
        run: |
          set -euo pipefail
          node --check trip-platform/functions/index.js
          node --check trip-platform/public/app.js
          node --test \
            trip-platform/functions/test/agenda-card-expansion-state-0506.test.js \
            trip-platform/functions/test/agenda-card-visual-parity-0473.test.js \
            trip-platform/functions/test/agenda-public-segment-availability-0484.test.js \
            trip-platform/functions/test/agenda-public-read-only-0475.test.js \
            trip-platform/functions/test/canonical-invalidation-0495.test.js \
            trip-platform/functions/test/agenda-public-timeline-reflection-0425.test.js \
            | tee /tmp/node-tests.log

      - name: Configure Firebase Android app for build only
        shell: bash
        run: |
          set -euo pipefail
          if [ -s app/google-services.json ]; then
            grep -Fq '"package_name": "br.com.mapeiaia.rotacerta"' app/google-services.json
            exit 0
          fi
          if [ -n "${FIREBASE_SERVICE_ACCOUNT:-}" ]; then
            printf '%s' "$FIREBASE_SERVICE_ACCOUNT" > /tmp/firebase-sa.json
            chmod 600 /tmp/firebase-sa.json
            export GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json
          elif [ -n "${GOOGLE_APPLICATION_CREDENTIALS_JSON:-}" ]; then
            printf '%s' "$GOOGLE_APPLICATION_CREDENTIALS_JSON" > /tmp/firebase-sa.json
            chmod 600 /tmp/firebase-sa.json
            export GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json
          fi
          PROJECT="${FIREBASE_PROJECT_ID:-rota-certa-7ccc8}"
          if [ -n "${FIREBASE_TOKEN:-}" ]; then
            npx --yes firebase-tools@latest apps:list ANDROID --project "$PROJECT" --token "$FIREBASE_TOKEN" > /tmp/apps.txt
            APP_ID="$(grep -oE '1:[0-9]+:android:[A-Za-z0-9]+' /tmp/apps.txt | head -1)"
            test -n "$APP_ID"
            npx --yes firebase-tools@latest apps:sdkconfig ANDROID "$APP_ID" --project "$PROJECT" --out app/google-services.json --token "$FIREBASE_TOKEN"
          else
            test -s /tmp/firebase-sa.json
            export GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-sa.json
            npx --yes firebase-tools@latest apps:list ANDROID --project "$PROJECT" > /tmp/apps.txt
            APP_ID="$(grep -oE '1:[0-9]+:android:[A-Za-z0-9]+' /tmp/apps.txt | head -1)"
            test -n "$APP_ID"
            npx --yes firebase-tools@latest apps:sdkconfig ANDROID "$APP_ID" --project "$PROJECT" --out app/google-services.json
          fi
          grep -Fq '"package_name": "br.com.mapeiaia.rotacerta"' app/google-services.json

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4

      - name: Run focused Android regressions
        shell: bash
        run: |
          set -euo pipefail
          chmod +x ./gradlew
          ./gradlew :app:testDebugUnitTest \
            --tests 'br.com.mapeiaia.rotacerta.trips.TimelineCanonicalBackend0494Test' \
            --tests 'br.com.mapeiaia.rotacerta.trips.CanonicalPassengerSegments0491Test' \
            --tests 'br.com.mapeiaia.rotacerta.trips.AgendaCanonicalProjectionConvergence0408Test' \
            --tests 'br.com.mapeiaia.rotacerta.trips.AgendaPublicTimelineReflection0425Test' \
            --tests 'br.com.mapeiaia.rotacerta.trips.PublicAgendaIncrementalAvailability0375Test' \
            --no-daemon --max-workers=1 --no-parallel --stacktrace | tee /tmp/android-tests.log

      - name: Assemble exact-head installable APK
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          ./gradlew :app:assembleDebug --no-daemon --max-workers=1 --no-parallel --stacktrace | tee /tmp/assemble.log
          APK='app/build/outputs/apk/debug/app-debug.apk'
          test -s "$APK"
          unzip -t "$APK" | tee /tmp/zip-integrity.txt
          BUILD_TOOLS="$(find "$ANDROID_HOME/build-tools" -maxdepth 1 -mindepth 1 -type d | sort -V | tail -1)"
          "$BUILD_TOOLS/aapt" dump badging "$APK" | tee /tmp/aapt.txt
          PACKAGE_LINE="$(grep '^package:' /tmp/aapt.txt | head -1)"
          grep -Fq "name='br.com.mapeiaia.rotacerta'" <<< "$PACKAGE_LINE"
          grep -Fq "versionCode='$EXPECTED_VERSION_CODE'" <<< "$PACKAGE_LINE"
          grep -Fq "versionName='$EXPECTED_VERSION_NAME'" <<< "$PACKAGE_LINE"
          "$BUILD_TOOLS/apksigner" verify --verbose --print-certs "$APK" | tee /tmp/apksigner.txt
          grep -Fq 'Verified' /tmp/apksigner.txt
          unzip -p "$APK" 'classes*.dex' | strings > /tmp/dex-strings.txt
          grep -Fq "$GITHUB_SHA" /tmp/dex-strings.txt

      - name: Configure Firebase deployment authentication
        shell: bash
        run: |
          set -euo pipefail
          if [ -n "${FIREBASE_SERVICE_ACCOUNT:-}" ]; then
            printf '%s' "$FIREBASE_SERVICE_ACCOUNT" > /tmp/firebase-deploy-sa.json
            chmod 600 /tmp/firebase-deploy-sa.json
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-deploy-sa.json" >> "$GITHUB_ENV"
            echo "FIREBASE_AUTH_MODE=service-account" >> "$GITHUB_ENV"
          elif [ -n "${GOOGLE_APPLICATION_CREDENTIALS_JSON:-}" ]; then
            printf '%s' "$GOOGLE_APPLICATION_CREDENTIALS_JSON" > /tmp/firebase-deploy-sa.json
            chmod 600 /tmp/firebase-deploy-sa.json
            echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-deploy-sa.json" >> "$GITHUB_ENV"
            echo "FIREBASE_AUTH_MODE=service-account" >> "$GITHUB_ENV"
          elif [ -n "${FIREBASE_TOKEN:-}" ]; then
            echo "FIREBASE_AUTH_MODE=token" >> "$GITHUB_ENV"
          else
            echo "Firebase deployment credential is missing."
            exit 3
          fi

      - name: Install Functions deployment dependencies
        shell: bash
        run: npm --prefix trip-platform/functions install --no-audit --no-fund

      - name: Deploy exact-head Agenda backend and hosting
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          PROJECT="${FIREBASE_PROJECT_ID:-rota-certa-7ccc8}"
          cd trip-platform
          if [ "${FIREBASE_AUTH_MODE:-}" = "token" ]; then
            npx --yes firebase-tools@latest deploy --project "$PROJECT" --only functions:trip-api,hosting --non-interactive --force --token "$FIREBASE_TOKEN"
          else
            npx --yes firebase-tools@latest deploy --project "$PROJECT" --only functions:trip-api,hosting --non-interactive --force
          fi

      - name: Smoke live Agenda expansion contract
        shell: bash
        run: |
          set -euo pipefail
          BASE=https://rota-certa-7ccc8.web.app
          curl --retry 10 --retry-delay 3 --fail --silent --show-error "$BASE/v1/health" | tee /tmp/live-health-0506.json
          grep -Fq '"ok":true' /tmp/live-health-0506.json

          propagated=false
          for attempt in $(seq 1 18); do
            BUST="$GITHUB_SHA-$attempt-$(date +%s%N)"
            curl --retry 3 --retry-delay 2 --fail --silent --show-error "$BASE/app.js?v=$BUST" -o /tmp/live-app-0506.js
            curl --retry 3 --retry-delay 2 --fail --silent --show-error "$BASE/?v=$BUST" -o /tmp/live-index-0506.html
            curl --retry 3 --retry-delay 2 --fail --silent --show-error "$BASE/v1/public/agenda/ezequiel?verify=$BUST" -o /tmp/live-agenda-0506.json
            if grep -Fq 'expandedAgendaTripKeys0506' /tmp/live-app-0506.js &&
               grep -Fq 'DATA_UPDATE_STATE_RESTORED' /tmp/live-app-0506.js &&
               grep -Fq 'TRIP_REMOVED' /tmp/live-app-0506.js &&
               grep -Fq 'app.js?v=0.1.506' /tmp/live-index-0506.html &&
               grep -Eq '"publicTripKey0506":"agenda-trip-[a-f0-9]{40}"' /tmp/live-agenda-0506.json; then
              propagated=true
              break
            fi
            sleep 5
          done
          test "$propagated" = true

          ! grep -Eiq '"(canonicalTripId|publicToken|passengerName|passengerContact|passengerId|sourceReference|fareMinorUnits|paymentStatus|boardingAddress|dropoffAddress|bookings|blablaTripId|whatsapp)"[[:space:]]*:' /tmp/live-agenda-0506.json
          grep -Fq 'function applyAgendaTripExpansionState0506' /tmp/live-app-0506.js
          grep -Fq 'expandedAgendaTripKeys0506.has(tripKey0506)' /tmp/live-app-0506.js
          grep -Fq '"USER_TOGGLE"' /tmp/live-app-0506.js
          printf 'LIVE_AGENDA_CARD_EXPANSION_0506=PASS sha=%s\n' "$GITHUB_SHA" | tee /tmp/live-smoke-0506.txt

      - name: Materialize exact-head APK and evidence
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/0506
          APK='app/build/outputs/apk/debug/app-debug.apk'
          cp "$APK" "/tmp/0506/rota-certa-$EXPECTED_VERSION_NAME-agenda-card-expansion.apk"
          sha256sum "/tmp/0506/rota-certa-$EXPECTED_VERSION_NAME-agenda-card-expansion.apk" | tee /tmp/0506/apk.sha256
          cp /tmp/source-head.txt /tmp/source-branch.txt /tmp/changed-files.txt /tmp/farol.sha256 /tmp/0506/
          cp /tmp/node-tests.log /tmp/android-tests.log /tmp/assemble.log /tmp/0506/
          cp /tmp/aapt.txt /tmp/apksigner.txt /tmp/zip-integrity.txt /tmp/dex-strings.txt /tmp/0506/
          cp /tmp/live-health-0506.json /tmp/live-app-0506.js /tmp/live-index-0506.html /tmp/live-agenda-0506.json /tmp/live-smoke-0506.txt /tmp/0506/
          cat > /tmp/0506/final-status.txt <<EOF
          agenda_card_expansion_state=CANONICAL_OPAQUE_KEY
          snapshot_update_collapses_card=false
          incremental_update_collapses_card=false
          periodic_refresh_collapses_card=false
          manual_reload_restores_expansion=true
          manual_user_collapse=true
          trip_removed_collapse=true
          data_updates_continue=true
          public_payload_private_ids=ABSENT
          timeline_collector_isolation=UNCHANGED
          capacity_logic=UNCHANGED
          farol_untouched=PASS
          deployment=PASS
          merge=false
          release=false
          latest=false
          package=br.com.mapeiaia.rotacerta
          version=$EXPECTED_VERSION_NAME/$EXPECTED_VERSION_CODE
          source_head=$GITHUB_SHA
          source_branch=$GITHUB_REF_NAME
          physical_approval=PENDING_SAMSUNG_SM_S911B_ANDROID_16
          EOF
          cat /tmp/0506/final-status.txt

      - uses: actions/upload-artifact@v4
        with:
          name: rota-certa-0.1.506-agenda-card-expansion-${{ github.sha }}
          retention-days: 30
          if-no-files-found: error
          path: /tmp/0506
