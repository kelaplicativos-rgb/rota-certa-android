name: Apply Saved Place Naming Flow (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow retired
        run: echo "Historical saved-place naming materializer retired; preserved in Git history and excluded from automatic pushes."
