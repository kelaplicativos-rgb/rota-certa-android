name: Passenger timeline geo UX causal patch materializer (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow retired
        run: echo "Historical one-shot materializer retired; preserved in Git history and excluded from automatic pushes."
