// No-op mantido temporariamente para compatibilidade do build.
// A antiga injecao de marcadores legados foi removida; este arquivo fica vazio
// ate a linha apply correspondente ser retirada com seguranca do build.gradle.kts.
