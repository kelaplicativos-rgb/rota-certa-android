(function(){const clean=(v)=>(v||'').replace(/\s+/g,' ').trim();const wanted={{DRIVER_NAME}};const links=Array.from(document.querySelectorAll('a[href]'));const a=links.find((n)=>clean(n.innerText)===wanted&&/profile|user|member/i.test(n.href||''));if(!a||typeof a.click!=='function')return JSON.stringify({found:false,clicked:false});const href=a.href||'';a.click();return JSON.stringify({found:true,clicked:true,href});})();
